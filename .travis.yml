language: python
python:
  - "3.7"
before_install:
  - pip install poetry
install:
  - poetry install
script: poetry run pytest --flake8 --cov=aiger -n2
cache:
  directories:
    - ${HOME}/.cache/pip
after_success:
  - codecov
